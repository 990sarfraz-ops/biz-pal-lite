document.addEventListener("DOMContentLoaded", function() {
    const container = document.createElement("div");
    container.style.width = "90%";
    container.style.margin = "20px auto";
    container.style.padding = "20px";
    container.style.borderRadius = "15px";
    container.style.background = "linear-gradient(to right, #f0f4f7, #c3cfe2)";
    container.style.boxShadow = "0 4px 15px rgba(0,0,0,0.1)";
    container.style.fontFamily = "Arial, sans-serif";

    const title = document.createElement("h2");
    title.innerText = "AI Business Video Assistant";
    title.style.textAlign = "center";
    title.style.color = "#333";

    const topicInput = document.createElement("input");
    topicInput.id = "videoTopic";
    topicInput.placeholder = "اپنے بزنس کا موضوع لکھیں...";
    topicInput.style.width = "100%";
    topicInput.style.padding = "12px";
    topicInput.style.margin = "10px 0";
    topicInput.style.borderRadius = "10px";
    topicInput.style.border = "1px solid #ccc";

    const generateBtn = document.createElement("button");
    generateBtn.innerText = "Generate Video Script";
    generateBtn.style.width = "100%";
    generateBtn.style.padding = "12px";
    generateBtn.style.border = "none";
    generateBtn.style.borderRadius = "10px";
    generateBtn.style.backgroundColor = "#4CAF50";
    generateBtn.style.color = "#fff";
    generateBtn.style.fontSize = "16px";
    generateBtn.style.cursor = "pointer";

    const outputArea = document.createElement("textarea");
    outputArea.id = "videoOutput";
    outputArea.rows = 12;
    outputArea.style.width = "100%";
    outputArea.style.padding = "10px";
    outputArea.style.borderRadius = "10px";
    outputArea.style.border = "1px solid #ccc";
    outputArea.style.marginTop = "10px";
    outputArea.readOnly = true;

    const copyBtn = document.createElement("button");
    copyBtn.innerText = "Copy Script";
    copyBtn.style.width = "100%";
    copyBtn.style.padding = "12px";
    copyBtn.style.marginTop = "10px";
    copyBtn.style.border = "none";
    copyBtn.style.borderRadius = "10px";
    copyBtn.style.backgroundColor = "#2196F3";
    copyBtn.style.color = "#fff";
    copyBtn.style.fontSize = "16px";
    copyBtn.style.cursor = "pointer";

    // AI Assistant logic
    generateBtn.onclick = function() {
        const topic = topicInput.value;
        if(topic.trim() === "") {
            alert("Please enter your business topic!");
            return;
        }

        // Generate step-by-step business video script
        const scriptSteps = [
            `🎬 Intro: Welcome to ${topic} Business Insights`,
            `📌 Step 1: Introduce your product/service for ${topic}`,
            `📌 Step 2: Highlight key benefits for customers`,
            `📌 Step 3: Provide real-life example or testimonial`,
            `📌 Step 4: Call-to-Action: Encourage viewers to contact or purchase`,
            `✅ Conclusion: Thank your audience and invite them to follow for updates`
        ];

        outputArea.value = scriptSteps.join("\n\n");
    };

    copyBtn.onclick = function() {
        navigator.clipboard.writeText(outputArea.value);
        alert("Video script copied successfully!");
    };

    container.appendChild(title);
    container.appendChild(topicInput);
    container.appendChild(generateBtn);
    container.appendChild(outputArea);
    container.appendChild(copyBtn);

    document.body.appendChild(container);
});
